RewriteEngine On

# Если запрос не указывает на существующий файл или директорию
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Перенаправляем на api.php
RewriteRule ^(.*)$ api.php [QSA,L]

